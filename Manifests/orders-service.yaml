apiVersion: v1
kind: Service
metadata:
  name: orders-service
  namespace: storefront
  labels:
    app: orders-service
spec:
  selector:
    app: orders-service
  ports:
    - name: http-metrics
      port: 8080
      targetPort: 8080
